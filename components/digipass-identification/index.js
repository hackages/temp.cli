const crelanIdentifier = 'crelan.identifier';
angular.module(crelanIdentifier, []);

document.addEventListener('DOMContentLoaded', function () {
  angular.bootstrap(document.body, [crelanIdentifier]);
});
